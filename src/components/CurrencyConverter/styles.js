export default theme => {
  return {

    root: {
      width: '100%'
    },

    rate: {
      fontSize: '1.1rem',
      marginRight: theme.spacing.unit * 2
    },

    paper: {
      marginBottom: theme.spacing.unit
    }

  };
};
