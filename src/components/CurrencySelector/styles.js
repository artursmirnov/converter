export default theme => {
  return {

    root: {

    },

    avatar: {
      fontSize: '0.9rem',
      border: `1px solid ${theme.palette.grey[900]}`
    },

    filter: {
      margin: `0 ${theme.spacing.unit * 3}px`
    }

  };
};
